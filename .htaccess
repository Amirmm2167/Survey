# Deny access to sensitive directories
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^(config|core)/ - [F,L]
</IfModule>

# Fallback for systems without mod_rewrite
<IfModule !mod_rewrite.c>
  Order allow,deny
  Deny from all
</IfModule>
